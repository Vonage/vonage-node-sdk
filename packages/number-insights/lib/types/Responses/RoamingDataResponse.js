/**
 * Type representing information about the roaming status of a phone number.
 * Vonage API's will return information using `snake_case`. This represents the
 * pure response before the client will transform the keys into `camelCase`.
 *
 * @typedef {Object} RoamingDataResponse
 * @property {string} status
 * @property {string} roaming_country_code
 * @property {string} roaming_network_code
 * @property {string} roaming_network_name
 */

export {};
