/**
 * Basic lookup options for a phone number lookup operation.
 *
 * @typedef {Object} BasicLookupOptions
 * @property {string} [country] - The two-character country code for the phone number's country.
 */

export {};
