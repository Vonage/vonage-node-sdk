/**
 * Type representing an SMS message.
 * Extends the structure of an SMS message response and includes additional optional properties.
 *
 * @typedef {Object} Message
 * @property {string} [messageId] - The unique ID of the SMS message (optional).
 * @property {string} [remainingBalance] - Your estimated remaining balance after sending the SMS (optional).
 * @property {string} [messagePrice] - The estimated cost of the SMS message (optional).
 * @property {string} [clientRef] - Your client reference for the message (optional).
 * @property {string} [accountRef] - An optional string used to identify separate accounts using the SMS endpoint for billing purposes (optional).
 */

export {};
