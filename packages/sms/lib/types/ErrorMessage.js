/**
 * Interface representing an error message.
 * Describes the structure of an error message containing status and error text.
 *
 * @typedef {Object} ErrorMessage
 * @property {string} [status] - The status code indicating the error (optional).
 * @property {string} [errorText] - The error text providing details about the error (optional).
 */

export {};
