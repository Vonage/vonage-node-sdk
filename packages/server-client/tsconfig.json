{
    "extends": "../../tsconfig.json",

    "compilerOptions": {
        "outDir": "dist"
    },

    "exclude": [
        "jest.config.js",
        "dist"
    ],

    "references": [
        { "path": "../auth" },
        { "path": "../jwt" },
        { "path": "../vetch" }
    ],

    "ts-node": {
        "esm": true
    }
}
