/**
 * Represents the parameters required for generating a signed hash.
 *
 * @typedef {Object} SignedHashParams
 * @property {string} secret - The secret key used to sign the hash, ensuring the integrity and authenticity of the message. It should be kept private and secure.
 * @property {AlgorithmTypes} algorithm - Specifies the algorithm type used for signing the hash. Utilizes the algorithm types defined in the `AlgorithmTypes` enum.
 */

export {};
