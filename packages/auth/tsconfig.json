{
    "extends": "../../tsconfig.json",

    "compilerOptions": {
        "rootDir": "lib",
        "outDir": "dist"
    },

    "exclude": [
        "__tests__",
        "jest.config.js",
        "dist"
    ],

    "references": [
        { "path": "../jwt" }
    ],

    "ts-node": {
        "esm": true
    }
}
