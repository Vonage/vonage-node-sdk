{
    "extends": "../../tsconfig.json",

    "compilerOptions": {
        "outDir": "dist"
    },

    "exclude": [
        "__tests__",
        "jest.config.js",
        "dist"
    ],

    "references": [
        { "path": "../jwt" }
    ],

    "ts-node": {
        "esm": true
    }
}
