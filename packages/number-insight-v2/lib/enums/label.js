/**
 * Enum representing the labels for risk scores.
 * @enum {string}
 */
export const Label = Object.freeze({
  /**
     * Represents a low risk score.
     */
  LOW: 'low',
  /**
     * Represents a medium risk score.
     */
  MEDIUM: 'medium',
  /**
     * Represents a high risk score.
     */
  HIGH: 'high'
});
