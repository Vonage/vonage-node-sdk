/**
 * Represents basic details of a call, including its UUID, conversation UUID, duration, and status.
 *
 * @typedef {Object} BasicCallDetail
 * @property {string} uuid - The UUID of the call.
 * @property {string} conversationUUID - The UUID of the conversation associated with the call.
 * @property {string} duration - The duration of the call in a string format.
 * @property {CallStatus} status - The status of the call, indicating its current state.
 */

export {};
