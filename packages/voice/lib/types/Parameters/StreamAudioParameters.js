/**
 * Represents parameters for streaming audio in a call.
 *
 * @typedef {Object} StreamAudioParameters
 * @property {Array} streamUrl - An array of URLs specifying the audio streams to be played in the call.
 * @property {number} [loop] - The number of times to loop the audio stream playback. Optional.
 * @property {number} [level] - The audio level or volume for the streamed audio. Optional.
 */

export {};
