/**
 * Represents parameters for playing DTMF (Dual-Tone Multi-Frequency) digits in a call.
 *
 * @typedef {Object} PlayDTMFParameters
 * @property {string} digits - The DTMF digits to be played in the call.
 */

export {};
