/**
 * Represents parameters for retrieving call details.
 *
 * @typedef {Object} GetCallDetailsParameters
 * @property {CallStatus} [status] - Optional: The status of the calls to retrieve.
 * @property {string} [dateStart] - Optional: The start date for filtering calls.
 * @property {string} [dateEnd] - Optional: The end date for filtering calls.
 * @property {number} [pageSize] - Optional: The page size for pagination.
 * @property {number} [recordIndex] - Optional: The record index for pagination.
 * @property {'asc' | 'desc'} [order] - Optional: The order in which calls are retrieved (ascending or descending).
 * @property {string} [conversationUuid] - Optional: The conversation UUID for filtering calls.
 */

export {};
