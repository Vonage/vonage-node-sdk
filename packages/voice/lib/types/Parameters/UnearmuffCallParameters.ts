/**
 * Represents parameters for unearmuffing a call, allowing the call audio to be heard again.
 */
export type UnearmuffCallParameters = {
  /**
   * The action type indicating unearmuffing of a call.
   */
  action: 'unearmuff';
};
