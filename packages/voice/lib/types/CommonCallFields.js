/**
 * Represents common fields for defining an outbound call, including the caller (from) and recipient (to) endpoints.
 *
 * @typedef {Object} CommonCallFields
 * @property {Omit} from - The caller's endpoint information, excluding DTMF answer details.
 * @property {CallEndpoint} to - The recipient's endpoint information for the call.
 */

export {};
