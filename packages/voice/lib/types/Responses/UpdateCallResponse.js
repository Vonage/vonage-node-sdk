/**
 * Represents the response for updating a call, including a message and the UUID of the call.
 * Vonage API's will return information using `snake_case`. This represents the
 * pure response before the client will transform the keys into `camelCase`.
 *
 * @typedef {Object} UpdateCallResponse
 * @property {string} message - A message associated with the call update response.
 * @property {string} uuid - The UUID of the call that was updated.
 */

export {};
