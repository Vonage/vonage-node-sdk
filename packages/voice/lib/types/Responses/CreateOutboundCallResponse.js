/**
 * Represents the response for creating an outbound call, including the conversation UUID and basic call details.
 * Vonage API's will return information using `snake_case`. This represents the
 * pure response before the client will transform the keys into `camelCase`.
 *
 * @typedef {Object} CreateOutboundCallResponse
 * @property {string} conversation_uuid - The UUID of the conversation associated with the outbound call.
 */

export {};
