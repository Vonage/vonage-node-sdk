/**
 * Represents a PSTN (Public Switched Telephone Network) endpoint, which can be used as a call destination.
 *
 * @typedef {Object} PSTNEndpoint
 * @property {'phone'} type - Specifies the type of endpoint, which is 'phone' for phone numbers.
 * @property {string} number - The phone number associated with the endpoint.
 * @property {string} [dtmfAnswer] - An optional DTMF (Dual-Tone Multi-Frequency) answer string. When provided, it enables automated call answering by sending DTMF tones.
 */

export {};
