/**
 * Connect the call to a RTC capable application
 *
 * @typedef {Object} AppEndpoint
 * @property {'app'} type - Specifies the type of endpoint, which is 'app' for RTC capable applications.
 * @property {string} user - The username of ther user to connect too. The username must have been added using {@link https://developer.nexmo.com/api/conversation#createUser}
 */

export {};
