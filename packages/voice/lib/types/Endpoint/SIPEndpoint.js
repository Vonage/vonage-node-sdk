/**
 * Represents a SIP (Session Initiation Protocol) endpoint, which can be used as a call destination.
 *
 * @typedef {Object} SIPEndpoint
 * @property {'sip'} type - Specifies the type of endpoint, which is 'sip' for SIP URIs.
 * @property {string} uri - The SIP URI associated with the endpoint.
 * @property {Record} [headers] - An optional array of headers as key-value pairs. These headers can be included in the SIP request.
 */

export {};
