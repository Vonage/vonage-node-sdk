/**
 * Represents advanced settings for machine detection during a call, including
 * behavior, mode, and optional beep timeout.
 *
 * @typedef {Object} AdvancedMachineDetection
 * @property {MachineDetectionBehavior} behavior - The behavior to follow when machine detection occurs.
 * @property {AdvancedMachineDetectionMode} mode - The mode of advanced machine detection.
 * @property {number} [beepTimeout] - (Optional) The timeout duration (in seconds) for detecting a beep during machine detection.
 */

export {};
