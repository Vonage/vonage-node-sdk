export enum CallStatus {
  STARTED = 'started',
  RINGING = 'ringing',
  ANSWERED = 'answered',
  MACHINE = 'machine',
  COMPLETED = 'completed',
  BUSY = 'busy',
  CANCELLED = 'cancelled',
  FAILED = 'failed',
  REJECTED = 'rejected',
  TIMEOUT = 'timeout',
  UNANSWERED = 'unanswered',
}
