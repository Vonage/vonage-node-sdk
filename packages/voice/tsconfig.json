{
    "extends": "../../tsconfig.json",

    "compilerOptions": {
        "outDir": "dist"
    },

    "exclude": [
        "__tests__",
        "jest.config.js",
        "dist"
    ],

    "references": [
        { "path": "../server-client" }
    ],

    "ts-node": {
        "esm": true
    }
}
