/**
 * Represents parameters for querying audit events.
 *
 * @typedef {Object} AuditParams
 * @property {AuditEventTypes | string} [eventType] - The type of the audit event to filter by.
 * @property {string} [dateFrom] - The start date for the audit event query.
 * @property {string} [dateTo] - The end date for the audit event query.
 * @property {string} [searchText] - Text to search within audit event descriptions.
 * @property {number} [page] - The page number for paginated results.
 * @property {number} [size] - The number of items per page.
 */

export {};
