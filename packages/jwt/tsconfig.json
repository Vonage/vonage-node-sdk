{
    "extends": "../../tsconfig.json",

    "compilerOptions": {
        "outDir": "dist"
    },

    "exclude": [
        "__tests__",
        "jest.config.js",
        "dist"
    ],

    "ts-node": {
        "esm": true
    }
}
