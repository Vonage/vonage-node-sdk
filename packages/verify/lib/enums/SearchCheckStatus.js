/**
 * Enum representing the status of a verification check during Verify search.
 * @enum {string}
 */
export const SearchCheckStatus = Object.freeze({
  /**
     * The verification check is invalid.
     */
  INVALID: 'INVALID',
  /**
     * The verification check is valid.
     */
  VALID: 'VALID'
});
