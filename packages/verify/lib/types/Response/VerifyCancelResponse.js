/**
 * Represents the response for cancelling a verification request.
 *
 * @typedef {Object} VerifyCancelResponse
 * @property {Command.CANCEL} command - The command type for the response, which is "CANCEL".
 */

export {};
