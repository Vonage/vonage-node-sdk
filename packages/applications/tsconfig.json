{
  "$schema": "https://json.schemastore.org/tsconfig",

  "extends": "../../tsconfig.json",

  "compilerOptions": {
    "rootDir": "lib",
    "outDir": "dist/lib"
  },

  "exclude": [
    "__tests__",
    "jest.config.js",
    "dist"
  ],

  "references": [
    { "path": "../server-client" }
  ],

  "ts-node": {
    "esm": true
  }
}
