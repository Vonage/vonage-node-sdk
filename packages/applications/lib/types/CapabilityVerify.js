/**
 * Represents a configuration for verification capabilities.
 *
 * @typedef {Object} CapabilityVerify
 * @property {Object} webhooks - Webhook configuration for verification status events. URL for handling verification status events.
 * @property {'v2'} version - The version of verification capabilities ('v2').
 */

export {};
