/**
 * Represents a configuration for messaging capabilities.
 *
 * @typedef {Object} CapabilityMessages
 * @property {Object} webhooks - Webhook configuration for inbound messages. URL for handling inbound messages. URL for handling message status events.
 * @property {'v1' | 'v0.1'} version - The version of messaging capabilities ('v1' or 'v0.1').
 * @property {boolean} authenticateInboundMedia - Whether to authenticate inbound media.
 */

export {};
