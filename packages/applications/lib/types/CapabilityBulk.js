/**
 * Represents a bulk capability configuration containing webhooks.
 *
 * @typedef {Object} CapabilityBulk
 * @property {Object} webhooks - Webhook configuration for proactive-connect related events. URL for listing status related to bulk operations. URL for running status related to bulk operations.
 */

export {};
