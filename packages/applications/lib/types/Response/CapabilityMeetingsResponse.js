/**
 * Represents the response for meetings-related capabilities configuration.
 * Vonage API's will return information using `snake_case`. This represents the
 * pure response before the client will transform the keys into `camelCase`
 *
 * @typedef {Object} CapabilityMeetingsResponse
 * @property {Object} webhooks - Webhook configuration for meetings-related events. Webhook for events related to changes in meeting rooms. Webhook for events related to changes in meeting sessions. Webhook for events related to changes in meeting recording.
 */

export {};
