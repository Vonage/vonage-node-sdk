/**
 * Represents the response for bulk-related capabilities configuration.
 * Vonage API's will return information using `snake_case`. This represents the
 * pure response before the client will transform the keys into `camelCase`
 *
 * @typedef {Object} CapabilityBulkResponse
 * @property {Object} webhooks - Webhook configuration for bulk events. Webhook for events related to bulk list status. Webhook for events related to bulk run status.
 */

export {};
