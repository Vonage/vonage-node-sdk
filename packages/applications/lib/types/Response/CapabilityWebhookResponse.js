/**
 * Represents the response for a capability webhook configuration.
 * Vonage API's will return information using `snake_case`. This represents the
 * pure response before the client will transform the keys into `camelCase`
 *
 * @typedef {Object} CapabilityWebhookResponse
 * @property {'POST' | 'GET'} http_method - The HTTP method to be used when sending data to the webhook endpoint. It can be either 'POST' or 'GET'.
 */

export {};
