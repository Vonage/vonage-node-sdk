/**
 * Represents the response for verification-related capabilities configuration.
 * Vonage API's will return information using `snake_case`. This represents the
 * pure response before the client will transform the keys into `camelCase`
 *
 * @typedef {Object} CapabilityVerifyResponse
 * @property {Object} webhooks - Webhook configuration for verification events. Webhook for events related to verification status.
 */

export {};
