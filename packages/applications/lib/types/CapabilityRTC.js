/**
 * Represents a configuration for RTC (Real-Time Communication) capabilities.
 *
 * @typedef {Object} CapabilityRTC
 * @property {Object} webhooks - Webhook configuration for RTC events. URL for handling RTC events.
 * @property {boolean} signedCallbacks - Whether to use signed callbacks for RTC.
 * @property {number} legPersistenceTime - The leg persistence time for RTC in milliseconds.
 */

export {};
