/**
 * Represents WhatsApp sticker parameters for sending stickers in a message.
 *
 * @typedef {Object} WhatsAppStickerParams
 * @property {WhatsAppStickerIdType | WhatsAppStickerUrlType} sticker - The sticker to be sent, which can be either a sticker ID or a sticker URL.
 */

export {};
