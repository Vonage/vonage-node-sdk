/**
 * Represents a WhatsApp template for sending structured template messages.
 *
 * @typedef {Object} WhatsAppTemplateType
 * @property {string} name - The name of the WhatsApp template.
 * @property {Array.<string>} parameters - An array of parameters for the WhatsApp template.
 */

export {};
