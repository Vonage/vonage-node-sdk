/**
 * Represents a WhatsApp policy type for deterministic messaging.
 *
 * @typedef {Object} WhatsAppPolicyType
 * @property {'deterministic'} policy - The policy type, which is set to 'deterministic'.
 * @property {WhatsAppLanguageCode} locale - The locale for the WhatsApp message, following the WhatsApp language code.
 */

export {};
