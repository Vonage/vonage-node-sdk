/**
 * @typedef {Object} RCSSuggestionParams
 * @property {RCSSuggestionType  text: string} type - The type for the suggestion object. The text shown on the suggestion chip and sent back if selected.
 * @property {string} postbackData - Developer-defined opaque metadata to return in the inbound webhook when the user selects this suggestion.
 */

export {};
