/**
 * Represents a message interface for image attachments.
 * This interface is used for defining messages with image attachments and
 * includes the message type 'image' and the image content.
 *
 * @typedef {Object} MessageImageInterface
 * @property {'image'} messageType - Specifies the message type as 'image'.
 * @property {MessageImageType} image - The image content of the message, including the URL to the image.
 */

export {};
