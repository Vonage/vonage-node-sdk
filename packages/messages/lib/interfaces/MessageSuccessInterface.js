/**
 * Represents a successful message delivery response.
 * This interface includes a property for the message UUID, which uniquely
 * identifies the sent message.
 *
 * @typedef {Object} MessageSuccessInterface
 * @property {string} messageUUID - The unique identifier (UUID) assigned to the sent message.
 */

export {};
