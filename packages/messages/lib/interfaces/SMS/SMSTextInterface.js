/**
 * Represents an interface for defining SMS channel text messages.
 *
 * @typedef {Object} SMSTextInterface
 * @property {'sms'} channel - The channel for SMS, which is always 'sms'.
 */

export {};
