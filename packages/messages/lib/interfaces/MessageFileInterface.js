/**
 * Represents a message interface for file attachments.
 * This interface is used for defining messages with file attachments and
 * includes the message type 'file' and the file content.
 *
 * @typedef {Object} MessageFileInterface
 * @property {'file'} messageType - Specifies the message type as 'file'.
 * @property {MessageFileType} file - The file content of the message, including the URL to the file and an optional caption.
 */

export {};
