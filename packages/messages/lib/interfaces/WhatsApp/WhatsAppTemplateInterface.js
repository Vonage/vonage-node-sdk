/**
 * Represents an interface for WhatsApp template messages.
 *
 * @typedef {Object} WhatsAppTemplateInterface
 * @property {'template'} messageType - Specifies the type of message, which is "template" for template messages.
 * @property {WhatsAppPolicyType} whatsapp - Defines the WhatsApp policy type for the template message.
 * @property {WhatsAppTemplateType} template - Specifies the details of the template to be sent, including its name and parameters.
 */

export {};
