/**
 * Represents an interface for WhatsApp sticker messages.
 *
 * @typedef {Object} WhatsAppStickerInterface
 * @property {'sticker'} messageType - Specifies the type of message, which is "sticker" for sticker messages.
 * @property {WhatsAppStickerUrlType | WhatsAppStickerIdType} sticker - The sticker to be sent. It can be either a sticker URL or a sticker ID.
 */

export {};
