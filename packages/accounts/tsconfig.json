{
    "extends": "../../tsconfig.json",

    "compilerOptions": {
        "outDir": "dist"
    },

    "exclude": [
        "jest.config.js",
        "dist"
    ],

    "references": [
        { "path": "../server-client" },
        { "path": "../auth" }
    ],

    "ts-node": {
        "esm": true
    }
}
