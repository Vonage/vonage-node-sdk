/**
 * Represents the response structure when updating account settings.
 * Settings were updated if supplied; the details of the current settings are included in the response.
 *
 * @typedef {Object} AccountUpdateResponse
 */

export {};
