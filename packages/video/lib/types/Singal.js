/**
 * Represents a signal containing type and data properties.
 *
 * @typedef {Object} Signal
 * @property {string} type - The type of the signal, which can be a custom string identifying the signal type.
 * @property {string} data - The data associated with the signal, which can be any string data related to the signal.
 */

export {};
