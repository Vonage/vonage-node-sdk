/**
 * Interface representing the configuration for updating a broadcast.
 *
 * @typedef {Object} BroadcastUpdateConfig
 * @property {string} broadcastId - The ID of the broadcast to update.
 * @property {boolean} [hasAudio] - Whether to include audio in the broadcast.
 * @property {boolean} [hasVideo] - Whether to include video in the broadcast.
 * @property {string} [addStream] - The ID of a stream to add to the broadcast.
 * @property {string} [removeStream] - The ID of a stream to remove from the broadcast.
 */

export {};
