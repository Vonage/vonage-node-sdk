/**
 * Represents a stream with associated layout class list.
 *
 * @typedef {Object} StreamClassList
 * @property {string} id - The ID of the stream.
 * @property {Array.<string>} layoutClassList - An array of layout class names associated with the stream.
 */

export {};
