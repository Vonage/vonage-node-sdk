/**
 * Interface representing filters for searching archives.
 *
 * @typedef {Object} ArchiveSearchFilter
 * @property {number} [offset] - The start offset in the list of existing archives.
 * @property {number} [count] - The number of archives to retrieve starting at the offset. Default is 50, with a maximum of 1000.
 * @property {string} [sessionId] - Retrieve archives for a specific session ID.
 */

export {};
