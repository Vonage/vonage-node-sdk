/**
 * Interface representing options for generating a client token.
 *
 * @typedef {Object} ClientTokenOptions
 * @property {string} [role] - The role associated with the client token (optional).
 * @property {string} [data] - Additional data for the client token (optional).
 * @property {number} [expireTime] - The expiration time of the client token in seconds (optional).
 * @property {Array.<string>} [initialLayoutClassList] - An array of initial layout class list for the client token (optional).
 */

export {};
