/**
 * Represents the request to initiate a SIP call with additional session information.
 *
 * @typedef {Object} InitiateSIPCallRequest
 * @property {string} sessionId - The session ID of the Vonage Video session to associate with the SIP call.
 */

export {};
