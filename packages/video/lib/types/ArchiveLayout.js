/**
 * Represents the layout configuration for an archive.
 *
 * @typedef {Object} ArchiveLayout
 * @property {LayoutType} type - The type of layout to be used for the archive.
 * @property {string} [stylesheet] - Optional: The stylesheet used for custom layout (only applicable if type is 'custom').
 * @property {string} [screenshareType] - Optional: The screenshare layout type to use when there is a screen-sharing stream in the archive (only applicable if type is 'bestFit' and screenshareType is set).
 */

export {};
