/**
 * Interface representing a filter for searching broadcasts.
 *
 * @typedef {Object} BroadcastSearchFilter
 * @property {number} [offset] - The offset for paginating the results.
 * @property {number} [count] - The maximum number of items to return.
 * @property {string} [sessionId] - The session ID to filter broadcasts by.
 */

export {};
