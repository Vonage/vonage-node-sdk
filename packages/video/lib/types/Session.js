/**
 * Interface representing a session configuration.
 *
 * @typedef {Object} Session
 * @property {string} sessionId - The unique identifier for the session.
 * @property {string} location - The location of the session.
 * @property {string} mediaMode - The media mode for the session (e.g., "ROUTED" or "RELAYED").
 * @property {string} archiveMode - The archive mode for the session (e.g., "MANUAL" or "ALWAYS").
 */

export {};
