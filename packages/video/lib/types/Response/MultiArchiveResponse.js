/**
 * Represents a response containing multiple archive items.
 *
 * @typedef {Object} MultiArchiveResponse
 * @property {number} count - The count of archive items in the response.
 * @property {Array.<SingleArchiveResponse>} items - An array of SingleArchiveResponse objects representing individual archive items.
 */

export {};
