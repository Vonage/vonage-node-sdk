/**
 * Represents the details of a single stream layout.
 *
 * @typedef {Object} SingleStreamLayoutResponse
 * @property {string} id - The unique identifier of the stream layout.
 * @property {string} videoType - The type of video associated with the stream layout.
 * @property {string} name - The name of the stream layout.
 * @property {Array.<string>} layoutClassList - An array of CSS class names associated with the layout.
 */

export {};
