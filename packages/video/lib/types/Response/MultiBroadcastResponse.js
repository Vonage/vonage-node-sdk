/**
 * Represents a response containing multiple broadcast details items.
 *
 * @typedef {Object} MultiBroadcastResponse
 * @property {number} count - The count of broadcast details items in the response.
 * @property {Array.<BroadcastDetailsResponse>} items - An array of BroadcastDetailsResponse objects representing individual broadcast details items.
 */

export {};
