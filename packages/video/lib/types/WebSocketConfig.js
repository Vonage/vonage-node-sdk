/**
 * Configuration options for establishing a WebSocket connection.
 *
 * @typedef {Object} WebSocketConfig
 * @property {string} uri - The URI to connect to the WebSocket server.
 * @property {Array.<string>} [streams] - An array of stream IDs to associate with the WebSocket connection.
 * @property {Object} [headers] - Optional headers to include in the WebSocket request.
 * @property {AudioRate} [audioRate] - The audio rate to be used for the WebSocket connection.
 * @property {boolean} [bidirectional] - Whether the WebSocket connection should be bidirectional. By default, this is false.
 */

export {};
