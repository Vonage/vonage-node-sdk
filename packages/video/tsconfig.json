{
    "extends": "../../tsconfig.json",

    "compilerOptions": {
        "rootDir": "lib",
        "outDir": "dist"
    },

    "exclude": [
        "__tests__",
        "jest.config.js",
        "dist"
    ],

    "references": [
        { "path": "../auth" },
        { "path": "../jwt" },
        { "path": "../server-client" },
        { "path": "../vetch" }
    ],

    "ts-node": {
        "esm": true
    }
}
