/**
 * @typedef {Object} MessageConversationBody
 * @property {string} id - The conversation id.
 * @property {string} name - The conversation name.
 * @property {string} [displayName] - The conversation display name.
 * @property {string} [imageUrl] - The image for the conversation.
 * @property {ConversationState} state - The conversation state.
 * @property {Object} [timestamp] - Timestamps for when the conversation was created, updated, and destroyed. @readonly The time the conversation was created. @readonly The time the conversation was last updated. @readonly The time the conversation was destroyed (if applicable). @readonly
 */

export {};
