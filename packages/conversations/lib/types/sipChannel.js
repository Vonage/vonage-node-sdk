/**
 * Type representing a SIP channel.
 *
 * @typedef {Object} SipChannel
 * @property {ChannelType.SIP} type - The type of channel (SIP).
 * @property {string} uri - The SIP URI.
 * @property {string} username - The SIP username.
 * @property {string} password - The SIP password.
 */

export {};
