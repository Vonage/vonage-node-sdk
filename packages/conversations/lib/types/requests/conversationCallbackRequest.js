/**
 * Type representing a callback object in a conversation request.
 *
 * @typedef {Object} ConversationCallbackRequest
 * @property {string} [url] - The callback URL.
 * @property {string} [event_mask] - The event mask for the callback.
 * @property {Object} [params] - Callback parameters. The application ID for the callback. The NCCO URL for the callback.
 * @property {'POST' | 'GET'} method - The callback method.
 */

export {};
