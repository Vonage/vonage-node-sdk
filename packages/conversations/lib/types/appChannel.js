/**
 * Type representing an app channel.
 *
 * @typedef {Object} AppChannel
 * @property {ChannelType.APP} type - The type of channel (app).
 * @property {string} user - The app user.
 */

export {};
