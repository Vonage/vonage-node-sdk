/**
 * Represents simple search options for phone numbers.
 *
 * @typedef {Object} NumbersSearchSimple
 * @property {string} [startsWith] - Filter numbers that start with a specific string.
 * @property {string} [endsWith] - Filter numbers that end with a specific string.
 * @property {string} [contains] - Filter numbers that contain a specific string.
 */

export {};
