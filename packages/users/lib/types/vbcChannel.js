/**
 * Represents a VBC (Voice Business Communication) channel with an extension.
 *
 * @typedef {Object} VbcChannel
 * @property {string} extension - The extension associated with the VBC channel.
 */

export {};
