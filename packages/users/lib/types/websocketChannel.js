/**
 * Represents a WebSocket channel configuration.
 *
 * @typedef {Object} WebsocketChannel
 * @property {string} uri - The WebSocket URI for the channel.
 * @property {string} [contentType] - Optional content type for WebSocket channel data.
 * @property {Record} [headers] - Optional headers for WebSocket channel configuration.
 */

export {};
