/**
 * Represents a SIP (Session Initiation Protocol) channel with the URI, username, and password.
 *
 * @typedef {Object} SipChannel
 * @property {string} uri - The SIP URI associated with the channel.
 * @property {string} username - The username for authentication with the SIP channel.
 * @property {string} password - The password for authentication with the SIP channel.
 */

export {};
