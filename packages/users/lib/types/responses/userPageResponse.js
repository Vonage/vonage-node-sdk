/**
 * Represents a page response containing a list of users.
 * Vonage API's will return information using `snake_case`. This represents the
 * pure response before the client will transform the keys into `camelCase`.
 *
 * @typedef {Object} UserPageResponse
 * @property {number} page_size - The number of records returned in this response.
 * @property {Object} _embedded - An object containing an array of user responses.
 */

export {};
