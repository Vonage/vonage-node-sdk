/**
 * Type representing possible errors related to verification requests.
 *
 * @typedef {Object} VerifyErrors
 * @property {'expired'} EXPIRED - Indicates that the verification request has expired.
 * @property {'conflict'} CONFLICT - Indicates a conflict error in the verification request.
 */

export {};
