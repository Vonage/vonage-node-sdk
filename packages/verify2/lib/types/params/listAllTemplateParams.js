/**
 * Query parameters for listing all templates in the Verify API.
 *
 * @typedef {Object} ListAllTemplatesParams
 * @property {number} [pageSize] - The number of templates to return per page. @example 10
 * @property {number} [page] - The page number to retrieve. @example 2
 */

export {};
