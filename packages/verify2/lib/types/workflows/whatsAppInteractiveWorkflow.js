/**
 * Represents a workflow for sending a verification code via interactive
 * WhatsApp.
 *
 * @typedef {Object} WhatsAppInteractiveWorkflow
 * @property {Channels.WHATSAPP_INTERACTIVE} channel - The communication channel for sending the verification code.
 * @property {string} to - The recipient's phone number where the verification code will be sent, typically in the E.164 format.
 */

export {};
