/**
 * Represents a workflow for sending a verification code via email.
 *
 * @typedef {Object} EmailWorkflow
 * @property {Channels.EMAIL} channel - The communication channel for sending the verification code.
 * @property {string} to - The recipient's email address where the verification code will be sent.
 * @property {string} [from] - (Optional) The sender's email address for the verification email.
 */

export {};
