/**
 * Represents a workflow for sending a verification code via WhatsApp.
 *
 * @typedef {Object} WhatsAppWorkflow
 * @property {Channels.WHATSAPP} channel - The communication channel for sending the verification code.
 * @property {string} to - The recipient's phone number where the verification code will be sent, typically in the E.164 format.
 * @property {string} [from] - (Optional) The sender's phone number or identifier for the WhatsApp message.
 */

export {};
