/**
 * Represents a workflow for sending a verification code via voice call.
 *
 * @typedef {Object} VoiceWorkflow
 * @property {Channels.VOICE} channel - The communication channel for sending the verification code.
 * @property {string} to - The recipient's phone number where the verification code will be sent, in the E.164 format.
 */

export {};
