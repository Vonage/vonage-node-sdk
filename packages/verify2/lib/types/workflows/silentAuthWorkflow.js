/**
 * Represents a workflow for Silent Authentication.
 *
 * @typedef {Object} SilentAuthWorkflow
 * @property {SilentAuthChannel} channel - The communication channel for Silent Authentication.
 * @property {string} to - The target identifier for Silent Authentication.
 * @property {string} redirectUrl - The redirect URL for Silent Authentication.
 */

export {};
