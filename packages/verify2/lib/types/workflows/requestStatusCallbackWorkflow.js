/**
 * Represents a workflow step within a verification request's status callback.
 *
 * @typedef {Object} RequestStatusCallbackWorkflow
 * @property {Channels} channel - The communication channel associated with this workflow step.
 * @property {string} initiated_at - The date and time when this workflow step was initiated in ISO 8601 format.
 * @property {RequestWorkflowStatus} status - The status of this workflow step within the verification request.
 */

export {};
