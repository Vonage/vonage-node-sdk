/**
 * Type definition representing a credit transfer, which includes properties such as `creditTransferId`, `amount`,
 * `from`, `to`, `reference`, and `createdAt`.
 *
 * @typedef {Object} CreditTransfer
 * @property {string} creditTransferId - The unique identifier for the credit transfer.
 * @property {number} amount - The amount of the credit transfer.
 * @property {string} from - The source account from which the credit is transferred.
 * @property {string} to - The destination account to which the credit is transferred.
 * @property {string} reference - A reference for the credit transfer.
 * @property {string} createdAt - The date and time when the credit transfer was executed.
 */

export {};
