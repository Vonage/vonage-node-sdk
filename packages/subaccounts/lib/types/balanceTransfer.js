/**
 * Type definition representing a balance transfer, which includes properties
 * such as `balanceTransferId`, `amount`, `from`, `to`, `reference`, and `createdAt`.
 *
 * @typedef {Object} BalanceTransfer
 * @property {string} balanceTransferId - The unique identifier for the balance transfer.
 * @property {number} amount - The amount of the balance transfer.
 * @property {string} from - The source account from which the balance is transferred.
 * @property {string} to - The destination account to which the balance is transferred.
 * @property {string} reference - A reference for the balance transfer.
 * @property {string} createdAt - The date and time when the balance transfer was executed.
 */

export {};
