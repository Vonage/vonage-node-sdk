/**
 * Type definition representing a subaccount, which includes properties such
 * as `primaryAccountApiKey` and `usePrimaryAccountBalance`, in addition to
 * properties from the `Account` type.
 *
 * @typedef {Object} SubAccount
 * @property {string} primaryAccountApiKey - The API key associated with the primary account.
 * @property {boolean} usePrimaryAccountBalance - Flag indicating whether to use the primary account balance (true) or not (false).
 */

export {};
