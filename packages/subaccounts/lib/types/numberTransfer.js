/**
 * Type definition representing a number transfer, which includes properties such as `number`, `country`, `from`, and
 * `to`.
 *
 * @typedef {Object} NumberTransfer
 * @property {string} number - The number being transferred.
 * @property {string} country - The two-character country code in ISO 3166-1 alpha-2 format.
 * @property {string} from - The source account from which the number is transferred.
 * @property {string} to - The destination account to which the number is transferred.
 */

export {};
