/**
 * Type definition for the response representing a page of credit transfers,
 * which includes an array of `credit_transfers`.
 * Vonage API's will return information using `snake_case`. This represents the
 * pure response before the client will transform the keys into `camelCase`.
 *
 * @typedef {Object} CreditTransferResponsePage
 * @property {Object} _embedded - An object containing an array of `credit_transfers`.
 */

export {};
