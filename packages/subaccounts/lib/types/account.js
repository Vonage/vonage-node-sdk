/**
 * Type definition representing an account, which includes properties such as
 * `apiKey`, `name`, `createdAt`, `suspended`, `balance`, and `creditLimit`.
 *
 * @typedef {Object} Account
 * @property {string} apiKey - The API key associated with the account.
 * @property {string} name - The name of the account.
 * @property {string} createdAt - The creation date and time of the account.
 * @property {boolean} suspended - The suspension status of the account.
 * @property {number} balance - The balance of the account.
 * @property {number} creditLimit - The credit limit of the account.
 */

export {};
