/**
 * Represents parameters for querying media items.
 *
 * @typedef {Object} MediaParameters
 * @property {'ascending' | 'descending'} [order] - The order of search results. Must be one of 'ascending' or 'descending'.
 * @property {number} [pageIndex] - Which page to retrieve in pagination.
 * @property {number} [pageSize] - How many items at most per page.
 * @property {string} [startTime] - Retrieve results created on or after this timestamp.
 * @property {string} [endTime] - Retrieve results created on or before this timestamp.
 */

export {};
