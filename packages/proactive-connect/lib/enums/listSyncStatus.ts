export enum SyncStatus {
    CONFIGURED = 'configured',
    CLEARING = 'clearing',
    FETCHING = 'fetching',
    PAUSED = 'paused',
    CANCELLED = 'cancelled',
    COMPLETED = 'completed',
    FAILED = 'failed',
}
