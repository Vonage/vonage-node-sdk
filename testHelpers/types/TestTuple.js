/**
 * TestTuple is a tuple that contains the name of the test suite and an array of SDKTestCase objects.
 *
 * @typedef {Object} TestTuple
 * @property {string} name - The name of the test suite. (Used for the describe block in the test file)
 * @property {Array>} tests - An array of SDKTestCase objects.
 */

export {};
